export function StackDisplay() {
  return (
    <div style={{ padding: '8px 12px', background: 'var(--color-surface)', borderRadius: 'var(--radius)', minHeight: '40px' }}>
      <span style={{ fontSize: '0.8rem', color: 'var(--color-text-muted)' }}>Stack (empty)</span>
    </div>
  );
}
